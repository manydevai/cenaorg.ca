RewriteEngine On

# Serve actual files directly (images, PDFs, JS, CSS, fonts, etc.)
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Everything else (including /magazine, /gallery SPA routes) -> React Router
RewriteRule ^ index.html [L]
